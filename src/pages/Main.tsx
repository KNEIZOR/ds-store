import React from 'react';

const Main = () => {
    const token = document.cookie
    console.log(token);
    

    return (
        <div>
            Main
        </div>
    );
};

export default Main;